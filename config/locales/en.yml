en:
  nodes: nodes
  add_new_node: add new node
  sort_by_name: sort by name

  activerecord:
    models:
      nodes/node:
        one: node
        other: nodes
    attributes:
      nodes/node:
        name: name
