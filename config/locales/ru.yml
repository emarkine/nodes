ru:
  nodes: nodes
  add_new_node: добавить новый узел
  sort_by_name: отсортировать по имени

  activerecord:
    models:
      nodes/node:
        one: узел
        other: узлы
    attributes:
      nodes/node:
        name: имя
