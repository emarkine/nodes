%h1= t(:nodes)
%table{:class => 'list'}
  %tr
    -#%th{colspan: 3}=link_to image_tag('nodes/create.png'), new_node_path, :title => t(:add_new,:model=>Node.model_name.human)
  %tr
    -#%th= link_to Node.human_attribute_name(:date), :action => :sort, :field => :date
    %th= link_to Nodes::Node.human_attribute_name(:name), :action => :sort, :field => :name
    -#%th= link_to Entry.human_attribute_name(:tags), :action => :sort, :field => :tag_list
  - @nodes.each do |node|
    %tr
      -#%td= l(node.date, :format=>:long)
      %td= link_to node.name,  edit_node_path # "/entry/#{entry.title}"
      -#%td
      -#  - entry.tags.each do |tag|
      -#    =link_to tag, tag_path(tag)
-#
-#
-#
-#%table
-#  %thead
-#    %tr
-#      %th Name
-#      %th
-#      %th
-#      %th
-#
-#  %tbody
-#    - @nodes.each do |node|
-#      %tr
-#        %td= node.name
-#        %td= link_to 'Show', node
-#        %td= link_to 'Edit', edit_node_path(node)
-#        %td= link_to 'Destroy', node, method: :delete, data: { confirm: 'Are you sure?' }
-#
-#%br
-#
-#= link_to 'New Node', new_node_path
